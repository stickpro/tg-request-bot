version: '3.5'

services:
  go-tgbot:
    image: go-tgbot
    container_name: vpn-wg
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 5050:5050
    volumes:
      - ./.bin/:/app/